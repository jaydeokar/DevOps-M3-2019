- job:
    name: checkBox Build
    description: 'My nodejs App'
    project-type: freestyle
    block-downstream: false
    scm:
    - git:
        skip-tag: false
        url: /home/ubuntu/checkBoxCode/
    auth-token: abcdefghi
    builders:
      - shell: |
          cd server-side/site
          npm install
          npm test
          cd /home/ubuntu
          sudo ansible-playbook -i hosts create_kube.yml
          sudo ansible-playbook -i hosts deployCheckbox/kube.yml
          sudo ansible-playbook -i hosts create_checkbox.yml
          sudo ansible-playbook -i hosts deployCheckbox/deployCheckbox.yml
