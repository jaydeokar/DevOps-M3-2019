---
  - name: replace line
    hosts: localhost
    tasks:
         - name: copy
           lineinfile:
                dest: /vagrant/variables.yml 
                regexp: '^(.*)CURL: (.*)$'
                line: 'CURL: "bhavya:8081/" '
                backrefs: yes                              